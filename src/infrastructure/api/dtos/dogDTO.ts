export interface BreedListDTO {
  message: Record<string, string[]>;
  status: string;
}

export interface SubBreedListDTO {
  message: string[];
  status: string;
}

export interface ImageListDTO {
  message: string[];
  status: string;
}
